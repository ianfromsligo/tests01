{
  "name": "postman-newman-jenkins",
  "version": "1.0.0",
  "description": "initial unit tests",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "api-tests-dev": "newman run tests/json-testsuite-basic-01.json --reporters junit --reporter-junit-export newman.xml"
  },
  "author": "Ian Craig",
  "dependencies": {
    "newman": "^3.10.0"
  }
}
